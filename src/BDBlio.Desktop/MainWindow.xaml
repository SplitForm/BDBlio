<Window x:Class="BDBlio.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BDBlio.Desktop"
        xmlns:ui="http://schemas.modernwpf.com"
        mc:Ignorable="d"
        Title="BDBlio - Gestionnaire de Bibliothèque" 
        Height="800" 
        Width="1200"
        Background="#1E1E1E"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Border Grid.Row="0" Background="#2D2D2D" BorderBrush="#6B5AED" BorderThickness="0,0,0,2" Padding="20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📚 BDBlio" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="#00D9FF" 
                           VerticalAlignment="Center"/>
                <TextBlock Text=" | Votre Bibliothèque de Bandes Dessinées" 
                           FontSize="16" 
                           Foreground="#B0B0B0" 
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Onglets -->
        <TabControl Grid.Row="1" 
                    Background="#1E1E1E" 
                    Foreground="#FFFFFF"
                    Padding="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="#2D2D2D"/>
                    <Setter Property="Foreground" Value="#B0B0B0"/>
                    <Setter Property="Padding" Value="20,10"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Grid>
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="#6B5AED" 
                                            BorderThickness="0,0,0,0"
                                            Name="Border">
                                        <ContentPresenter ContentSource="Header" 
                                                          Margin="{TemplateBinding Padding}" 
                                                          Cursor="Hand"/>
                                    </Border>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#3D3D3D"/>
                                        <Setter TargetName="Border" Property="BorderThickness" Value="0,3,0,0"/>
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#6B5AED"/>
                                        <Setter Property="Foreground" Value="#00D9FF"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#383838"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Onglet: Ajouter une BD -->
            <TabItem Header="➕ Ajouter une BD">
                <local:AddComicBookPage/>
            </TabItem>

            <!-- Onglet: Rechercher une BD -->
            <TabItem Header="🔍 Rechercher une BD">
                <local:SearchComicBookPage/>
            </TabItem>

            <!-- Onglet: Ma Bibliothèque -->
            <TabItem Header="📖 Ma Bibliothèque">
                <local:LibraryPage/>
            </TabItem>

            <!-- Onglet: Paramètres -->
            <TabItem Header="⚙️ Paramètres">
                <local:SettingsPage/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
